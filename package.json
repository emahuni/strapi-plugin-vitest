{
  "name": "strapi-plugin-vitest",
  "version": "0.1.0",
  "description": "Strapi plugin creates a Vitest unit testing harness, that loads Strapi in `strapi develop` allowing you to watch for changes and run tests from seperate tests files without reloading the singlton. You don't have to require your tests from the `app.test.js` file, each test file is independent of others.",
  "strapi": {
    "name": "strapi-plugin-vitest",
    "description": "Strapi plugin creates a Vitest unit testing harness, that loads Strapi in `strapi develop` allowing you to watch for changes and run tests from seperate tests files without reloading the singlton. You don't have to require your tests from the `app.test.js` file, each test file is independent of others.",
    "kind": "plugin"
  },
  "bin": "vitest-init",
  "dependencies": {
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "fs-extra": "^10.1.0",
    "lodash": "^4.17.21",
    "typescript": "4.6.3",
    "@vitest/browser": "^0.22.1",
    "@vitest/ui": "^0.22.1",
    "expect-more-jest": "^5.4.1",
    "jest-extended": "^3.0.2",
    "sinon": "^14.0.0",
    "sinon-chai": "^3.7.0",
    "vitest": "^0.22.1"
  },
  "author": {
    "name": "Emmanuel Mahuni",
    "email": "emahuni@gmail.com"
  },
  "repository": {
    "url": "https://github.com/emahuni/strapi-plugin-vitest"
  },
  "maintainers": [
    {
      "name": "Emmanuel Mahuni",
      "email": "emahuni@gmail.com"
    }
  ],
  "engines": {
    "node": ">=14.19.1 <=16.x.x",
    "npm": ">=6.0.0"
  },
  "scripts": {
    "develop": "tsc -w",
    "build": "tsc",
    "install": "tsc"
  },
  "license": "MIT"
}
