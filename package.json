{
  "name": "strapi-plugin-vitest",
  "version": "0.1.8",
  "description": "Strapi plugin creates a Vitest unit testing harness, that loads Strapi in `strapi develop` allowing you to watch for changes and run tests from separate tests files without reloading the singlton. You don't have to require your tests from the `app.test.js` file, each test file is independent of others.",
  "strapi": {
    "name": "strapi-plugin-vitest",
    "description": "Strapi plugin creates a Vitest unit testing harness, that loads Strapi in `strapi develop` allowing you to watch for changes and run tests from separate tests files without reloading the singlton. You don't have to require your tests from the `app.test.js` file, each test file is independent of others.",
    "kind": "plugin"
  },
  "bin": {
    "strapi-plugin-vitest-init": "bin/vitest-init"
  },
  "devDependencies": {
    "@types/node": "^18.7.14",
    "chalk": "^4.1.2",
    "chokidar": "^3.5.3",
    "expect-more-jest": "^5.4.1",
    "fs-extra": "^10.1.0",
    "jest-extended": "^3.0.2",
    "override.env": "^1.0.2",
    "sinon": "^14.0.0",
    "sinon-chai": "^3.7.0",
    "typescript": "4.6.3",
    "vitest": "^0.22.1",
    "yargs": "^17.5.1"
  },
  "author": {
    "name": "Emmanuel Mahuni",
    "email": "emahuni@gmail.com"
  },
  "repository": {
    "url": "https://github.com/emahuni/strapi-plugin-vitest"
  },
  "maintainers": [
    {
      "name": "Emmanuel Mahuni",
      "email": "emahuni@gmail.com"
    }
  ],
  "engines": {
    "node": ">=14.19.1 <=16.x.x",
    "npm": ">=6.0.0"
  },
  "scripts": {
    "develop": "tsc -w",
    "build": "tsc"
  },
  "license": "MIT"
}
